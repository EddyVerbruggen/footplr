// Add your native dependencies here:

android {
  defaultConfig {
    generatedDensities = []
    multiDexEnabled true
    applicationId = "com.footballplayerratings.app"
  }

  dexOptions {
    javaMaxHeapSize "4g"
  }

  aaptOptions {
    additionalParameters "--no-version-vectors"
  }
}
